# 📚 المحاضرة 2: بنية المشروع

**المدة**: 25 دقيقة  
**المستوى**: مبتدئ

---

## 🎯 أهداف المحاضرة

- ✅ فهم بنية مشروع الإضافة
- ✅ معرفة دور كل ملف
- ✅ تنظيم الكود بشكل احترافي

---

## 📁 البنية الكاملة

```
my-extension/
├── .vscode/              # إعدادات VSCode
│   ├── launch.json       # إعدادات التصحيح
│   ├── tasks.json        # مهام البناء
│   └── extensions.json   # إضافات موصى بها
│
├── src/                  # الكود المصدري
│   ├── extension.ts      # نقطة الدخول
│   ├── commands/         # الأوامر
│   ├── utils/            # أدوات مساعدة
│   └── types/            # أنواع TypeScript
│
├── out/                  # الكود المترجم (يُنشأ تلقائياً)
│   └── extension.js
│
├── node_modules/         # التبعيات
│
├── .gitignore           # ملفات Git المتجاهلة
├── .vscodeignore        # ملفات لا تُضمن في الحزمة
├── package.json         # معلومات الإضافة
├── package-lock.json    # قفل التبعيات
├── tsconfig.json        # إعدادات TypeScript
├── README.md            # الوثائق
├── CHANGELOG.md         # سجل التغييرات
└── LICENSE              # الرخصة
```

---

## 📄 الملفات الأساسية

### 1. package.json

الملف الأهم - يحتوي على:
- معلومات الإضافة
- الأوامر
- الإعدادات
- التبعيات

### 2. src/extension.ts

نقطة الدخول - يحتوي على:
- `activate()`: يُنفذ عند تفعيل الإضافة
- `deactivate()`: يُنفذ عند إيقاف الإضافة

### 3. tsconfig.json

إعدادات TypeScript:
- إصدار JavaScript المستهدف
- مجلد الإخراج
- خيارات الترجمة

---

## 🔧 .vscode/launch.json

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/out/**/*.js"
      ],
      "preLaunchTask": "${defaultBuildTask}"
    }
  ]
}
```

---

## 🎓 تمرين: تنظيم الكود

أنشئ هذه البنية:

```
src/
├── extension.ts
├── commands/
│   ├── helloWorld.ts
│   └── index.ts
└── utils/
    └── logger.ts
```

---

**التالي**: [المحاضرة 3: package.json](03-package-json.md)
