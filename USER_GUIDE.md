# 📖 دليل الاستخدام الشامل - Scrcpy Smart Connect v3.0.0

## 📑 جدول المحتويات

1. [المقدمة](#المقدمة)
2. [التثبيت](#التثبيت)
3. [البدء السريع](#البدء-السريع)
4. [الميزات الأساسية](#الميزات-الأساسية)
5. [إدارة الأجهزة المتعددة](#إدارة-الأجهزة-المتعددة)
6. [ملفات التعريف](#ملفات-التعريف)
7. [إعدادات الجودة](#إعدادات-الجودة)
8. [المراقبة والأداء](#المراقبة-والأداء)
9. [العمليات الجماعية](#العمليات-الجماعية)
10. [الميزات المتقدمة](#الميزات-المتقدمة)
11. [حل المشاكل](#حل-المشاكل)

---

## المقدمة

**Scrcpy Smart Connect** هو أداة قوية لإدارة الاتصال اللاسلكي بأجهزة Android عبر scrcpy. يوفر اتصالاً تلقائياً، إدارة أجهزة متعددة، وميزات متقدمة للمطورين.

### المتطلبات

- **scrcpy** - أداة عرض الشاشة
- **ADB** - Android Debug Bridge
- **جهاز Android** مع تفعيل USB debugging

### التثبيت السريع

```bash
# استنساخ المشروع
git clone https://github.com/tareq-alomari/scrcpy-smart.git
cd scrcpy-smart

# تشغيل المثبت
./install.sh
```

أو بأمر واحد:
```bash
curl -fsSL https://raw.githubusercontent.com/tareq-alomari/scrcpy-smart/main/install.sh | bash
```

---

## البدء السريع

### الاتصال الأول

1. **وصل جهازك عبر USB**
2. **فعّل USB debugging** على الجهاز
3. **شغّل الأداة**:

```bash
scrcpy-smart
# أو
phone
```

**ماذا يحدث؟**
- ✅ يكتشف الجهاز تلقائياً
- ✅ يحصل على عنوان IP
- ✅ يحوّل إلى وضع لاسلكي
- ✅ يحفظ IP للمرات القادمة

### الاتصالات التالية

بعد الاتصال الأول، فقط شغّل:
```bash
scrcpy-smart
```

**لا حاجة لكابل USB!** 🎉

---

## الميزات الأساسية

### 1. الأوامر الأساسية

#### عرض المساعدة
```bash
scrcpy-smart --help
```

#### معرفة الإصدار
```bash
scrcpy-smart --version
```

#### عرض الإعدادات الحالية
```bash
scrcpy-smart --config
```

#### إعادة تعيين IP المحفوظ
```bash
scrcpy-smart --reset
```

### 2. إدارة الأجهزة

#### عرض جميع الأجهزة المتصلة
```bash
scrcpy-smart --list
```

#### اختيار جهاز يدوياً
```bash
scrcpy-smart --select
```

#### الاتصال بـ IP محدد
```bash
scrcpy-smart --ip 192.168.1.100
```

#### استخدام منفذ مخصص
```bash
scrcpy-smart --port 5556
```

### 3. الوضع التفصيلي (Verbose)

للحصول على معلومات تفصيلية أثناء التشغيل:
```bash
scrcpy-smart --verbose
```

---

## إدارة الأجهزة المتعددة

### حفظ الأجهزة

احفظ أجهزتك بأسماء مستعارة للوصول السريع:

```bash
# حفظ الجهاز الحالي
scrcpy-smart --save myphone

# حفظ جهاز آخر
scrcpy-smart --save tablet

# حفظ جهاز العمل
scrcpy-smart --save work-phone
```

### الاتصال بجهاز محفوظ

```bash
scrcpy-smart --device myphone
```

### عرض جميع الأجهزة المحفوظة

```bash
scrcpy-smart --devices
```

**مثال على الناتج:**
```
📱 Saved Devices:

• myphone
  IP: 192.168.1.100

• tablet
  IP: 192.168.1.101

• work-phone
  IP: 192.168.1.102
```

### إعادة تسمية جهاز

```bash
scrcpy-smart --rename myphone personal-phone
```

### حذف جهاز

```bash
scrcpy-smart --remove myphone
```

### أمثلة عملية

```bash
# الاتصال بالتابلت مع ملف تعريف التسجيل
scrcpy-smart --device tablet --profile recording

# الاتصال بهاتف العمل بوضع ملء الشاشة
scrcpy-smart --device work-phone --fullscreen

# الاتصال بجهاز محدد بدون صوت
scrcpy-smart --device myphone --no-audio
```

---

## ملفات التعريف

ملفات التعريف هي إعدادات محسّنة لحالات استخدام مختلفة.

### الملفات المتاحة

#### 1. Gaming (الألعاب)
```bash
scrcpy-smart --profile gaming
```
- **الدقة**: 720p
- **معدل البت**: 4M
- **FPS**: 120
- **مثالي لـ**: ألعاب سريعة، استجابة فورية

#### 2. Recording (التسجيل)
```bash
scrcpy-smart --profile recording
```
- **الدقة**: 1920p
- **معدل البت**: 16M
- **FPS**: 60
- **مثالي لـ**: تسجيل فيديوهات عالية الجودة

#### 3. Demo (العروض التقديمية)
```bash
scrcpy-smart --profile demo
```
- **الدقة**: 1024p
- **معدل البت**: 8M
- **FPS**: 60
- **خيارات**: بدون حدود، دائماً في المقدمة
- **مثالي لـ**: العروض التقديمية والديمو

#### 4. Battery (توفير البطارية)
```bash
scrcpy-smart --profile battery
```
- **الدقة**: 720p
- **معدل البت**: 2M
- **FPS**: 30
- **خيارات**: إطفاء الشاشة
- **مثالي لـ**: الاستخدام الطويل، توفير البطارية

#### 5. Dev (التطوير)
```bash
scrcpy-smart --profile dev
```
- **الدقة**: 1024p
- **معدل البت**: 6M
- **FPS**: 60
- **خيارات**: إبقاء الشاشة مضاءة
- **مثالي لـ**: جلسات البرمجة الطويلة

#### 6. Presentation (العرض)
```bash
scrcpy-smart --profile presentation
```
- **الدقة**: 1280p
- **معدل البت**: 10M
- **FPS**: 60
- **خيارات**: ملء الشاشة، بدون حدود
- **مثالي لـ**: العروض التقديمية الاحترافية

### دمج الملفات مع خيارات أخرى

```bash
# ألعاب بدون صوت
scrcpy-smart --profile gaming --no-audio

# تسجيل بملء الشاشة
scrcpy-smart --profile recording --fullscreen

# عرض تقديمي مع إشعارات
scrcpy-smart --profile presentation --notify
```

---

## إعدادات الجودة

إعدادات سريعة للجودة حسب سرعة الشبكة.

### الإعدادات المتاحة

#### Low (منخفضة)
```bash
scrcpy-smart --quality low
```
- 480p, 2M, 30fps
- **مثالي لـ**: شبكات ضعيفة

#### Medium (متوسطة)
```bash
scrcpy-smart --quality medium
```
- 720p, 4M, 60fps
- **مثالي لـ**: شبكات عادية

#### High (عالية)
```bash
scrcpy-smart --quality high
```
- 1080p, 8M, 60fps
- **مثالي لـ**: شبكات قوية

#### Ultra (فائقة)
```bash
scrcpy-smart --quality ultra
```
- 1440p, 16M, 60fps
- **مثالي لـ**: شبكات 5GHz قوية جداً

---

## التحكم بالعرض

### تدوير الشاشة

```bash
# بدون تدوير
scrcpy-smart --rotate 0

# تدوير 90 درجة
scrcpy-smart --rotate 90

# تدوير 180 درجة
scrcpy-smart --rotate 180

# تدوير 270 درجة
scrcpy-smart --rotate 270
```

### قص الشاشة

```bash
# قص منطقة محددة
scrcpy-smart --crop 1920x1080:0:0
```

### خيارات العرض السريعة

```bash
# ملء الشاشة
scrcpy-smart --fullscreen

# بدون صوت
scrcpy-smart --no-audio

# تسجيل إلى ملف
scrcpy-smart --record demo.mp4
```

---

## معلومات الجهاز

### عرض معلومات شاملة

```bash
scrcpy-smart --info
```

**مثال على الناتج:**
```
📱 Device Information

Device: 192.168.1.100:5555

📱 Model: Samsung Galaxy S21
🤖 Android: 12
📦 SDK: 31
📐 Resolution: 1080x2400
🔋 Battery: 85%
🌐 IP: 192.168.1.100
```

---

## المراقبة والأداء

### مراقبة الأداء

```bash
scrcpy-smart --perf
```

**يعرض:**
- 🔋 مستوى البطارية
- ⚡ استخدام CPU
- 💾 استخدام الذاكرة
- 📱 حالة الشاشة

### المراقبة التلقائية

#### وضع المراقبة (Foreground)
```bash
scrcpy-smart --monitor
```
- يراقب الاتصال
- يعيد الاتصال تلقائياً عند الانقطاع

#### وضع Daemon (Background)
```bash
# تشغيل كخدمة خلفية
scrcpy-smart --daemon

# التحقق من الحالة
scrcpy-smart --status

# عرض السجلات
scrcpy-smart --logs

# مسح السجلات
scrcpy-smart --clear-logs

# إيقاف الخدمة
scrcpy-smart --stop
```

---

## الإشعارات

### تفعيل الإشعارات

```bash
scrcpy-smart --notify
```

**ستحصل على إشعارات عند:**
- ✅ الاتصال الناجح
- ❌ فشل الاتصال
- 🔍 اكتشاف جهاز جديد
- ⚠️ الأخطاء

### دمج مع ميزات أخرى

```bash
# مراقبة مع إشعارات
scrcpy-smart --monitor --notify

# daemon مع إشعارات
scrcpy-smart --daemon --notify
```

---

## وضع المراقبة (Watch Mode)

انتظار الجهاز والاتصال تلقائياً عند ظهوره.

```bash
# انتظار لمدة 30 ثانية (افتراضي)
scrcpy-smart --watch

# انتظار لمدة 60 ثانية
scrcpy-smart --watch --timeout 60

# مع إشعارات
scrcpy-smart --watch --notify
```

**مثالي لـ:**
- انتظار إعادة تشغيل الجهاز
- الاتصال التلقائي عند توصيل الجهاز
- الإعدادات التلقائية

---

## فحص الشبكة

### البحث عن أجهزة على الشبكة

```bash
scrcpy-smart --scan
```

**يفحص:**
- جميع عناوين IP على الشبكة المحلية (254 عنوان)
- يعرض الأجهزة المكتشفة
- يحفظ النتائج

---

## الإجراءات السريعة

### لقطة شاشة

```bash
scrcpy-smart --screenshot
```
- يحفظ بصيغة: `screenshot_YYYYMMDD_HHMMSS.png`

### إعادة تشغيل الجهاز

```bash
scrcpy-smart --reboot
```

### تثبيت APK

```bash
scrcpy-smart --install app.apk
```

### فتح ADB Shell

```bash
scrcpy-smart --shell
```

---

## العمليات الجماعية

تنفيذ أوامر على جميع الأجهزة المحفوظة.

### تنفيذ أمر

```bash
scrcpy-smart --batch 'pm list packages'
```

### رفع ملف لجميع الأجهزة

```bash
scrcpy-smart --push app.apk
```

### تنزيل ملف من جميع الأجهزة

```bash
scrcpy-smart --pull /sdcard/log.txt
```

**مثال عملي:**
```bash
# تثبيت تطبيق على جميع أجهزة الاختبار
scrcpy-smart --push myapp.apk
scrcpy-smart --batch 'pm install /sdcard/myapp.apk'

# جمع السجلات من جميع الأجهزة
scrcpy-smart --batch 'logcat -d > /sdcard/log.txt'
scrcpy-smart --pull /sdcard/log.txt
```

---

## الحافظة (Clipboard)

### نسخ نص إلى الجهاز

```bash
scrcpy-smart --copy "Hello Device"
```

### لصق نص من الجهاز

```bash
scrcpy-smart --paste
```

---

## ملفات الإعدادات المخصصة

### حفظ إعدادات مخصصة

```bash
# حفظ الإعدادات الحالية
scrcpy-smart --save-profile myconfig
```

### تحميل إعدادات محفوظة

```bash
scrcpy-smart --load-profile myconfig
```

### عرض جميع الملفات المحفوظة

```bash
scrcpy-smart --list-profiles
```

---

## النسخ الاحتياطي والاستعادة

### نسخ احتياطي

```bash
# نسخ احتياطي إلى مجلد محدد
scrcpy-smart --backup ./my-backup

# نسخ احتياطي تلقائي (مع تاريخ)
scrcpy-smart --backup
```

### استعادة

```bash
scrcpy-smart --restore ./my-backup
```

**⚠️ تحذير:** الاستعادة ستستبدل بيانات الجهاز!

---

## الاختصارات المخصصة (Aliases)

### إنشاء اختصار

```bash
scrcpy-smart --alias quick 'scrcpy-smart --profile gaming --fullscreen'
```

### تشغيل اختصار

```bash
scrcpy-smart --run-alias quick
```

### عرض جميع الاختصارات

```bash
scrcpy-smart --list-aliases
```

**أمثلة مفيدة:**
```bash
# اختصار للعمل
scrcpy-smart --alias work 'scrcpy-smart --device work-phone --profile dev'

# اختصار للألعاب
scrcpy-smart --alias game 'scrcpy-smart --profile gaming --no-audio'

# اختصار للتسجيل
scrcpy-smart --alias rec 'scrcpy-smart --profile recording --record video.mp4'
```

---

## QR Code Pairing

### توليد QR Code

```bash
scrcpy-smart --qr
```

**يعرض:**
- QR code للمسح
- عنوان IP والمنفذ
- أمر الاقتران المباشر

### الاقتران باستخدام IP

```bash
scrcpy-smart --pair 192.168.1.100
```

**متطلبات:**
- تثبيت `qrencode` لعرض QR code
- الجهاز والكمبيوتر على نفس الشبكة

---

## الواجهة الويب

### تشغيل الواجهة

```bash
./scrcpy-web.sh

# أو على منفذ مخصص
./scrcpy-web.sh 8080
```

**الوصول:**
```
http://localhost:8080/scrcpy-web.html
```

**الميزات:**
- ✅ أزرار سريعة للاتصال
- ✅ اختيار ملفات التعريف
- ✅ عرض معلومات الجهاز
- ✅ مراقبة الأداء
- ✅ فحص الشبكة

---

## نظام الإضافات (Plugins)

### تشغيل إضافة

```bash
scrcpy-smart --plugin auto-screenshot
```

### عرض الإضافات المتاحة

```bash
scrcpy-smart --list-plugins
```

### إنشاء إضافة مخصصة

1. **أنشئ ملف في** `~/.scrcpy-plugins/`:

```bash
nano ~/.scrcpy-plugins/my-plugin.sh
```

2. **اكتب السكريبت:**

```bash
#!/bin/bash
echo "🔌 My Custom Plugin"

# كودك هنا
scrcpy-smart --info
scrcpy-smart --perf
```

3. **اجعله قابل للتنفيذ:**

```bash
chmod +x ~/.scrcpy-plugins/my-plugin.sh
```

4. **شغّله:**

```bash
scrcpy-smart --plugin my-plugin
```

---

## ملف الإعدادات

### إنشاء ملف إعدادات

```bash
nano ~/.scrcpy-smart.conf
```

### مثال على الإعدادات:

```bash
# إعدادات الجودة الافتراضية
DEFAULT_BITRATE=8M
DEFAULT_SIZE=1024
DEFAULT_FPS=60

# الإعدادات العامة
VERBOSE=false
AUTO_RECONNECT=true
RECONNECT_INTERVAL=5
ERROR_LOG=true
NOTIFY=false

# الجهاز المفضل
PREFERRED_DEVICE=192.168.1.100
```

---

## أمثلة عملية شاملة

### للمطورين

```bash
# جلسة تطوير كاملة
scrcpy-smart --device work-phone --profile dev --notify

# مع مراقبة تلقائية
scrcpy-smart --device work-phone --profile dev --monitor --notify

# تثبيت وتشغيل
scrcpy-smart --install app.apk
scrcpy-smart --device myphone
```

### للاعبين

```bash
# ألعاب بأفضل أداء
scrcpy-smart --profile gaming --fullscreen --no-audio

# مع جودة منخفضة للشبكات الضعيفة
scrcpy-smart --profile gaming --quality low --fullscreen
```

### للمسجلين

```bash
# تسجيل عالي الجودة
scrcpy-smart --profile recording --record demo.mp4

# مع جودة فائقة
scrcpy-smart --quality ultra --record presentation.mp4
```

### لفرق الاختبار

```bash
# تثبيت على جميع الأجهزة
scrcpy-smart --push app.apk
scrcpy-smart --batch 'pm install /sdcard/app.apk'

# جمع السجلات
scrcpy-smart --batch 'logcat -d > /sdcard/test.log'
scrcpy-smart --pull /sdcard/test.log

# لقطات شاشة من جميع الأجهزة
scrcpy-smart --batch 'screencap -p /sdcard/screen.png'
scrcpy-smart --pull /sdcard/screen.png
```

---

## حل المشاكل

### المشكلة: لا يتم اكتشاف الجهاز

**الحلول:**
```bash
# 1. تحقق من USB debugging
adb devices

# 2. أعد تشغيل ADB
adb kill-server
adb start-server

# 3. أعد تعيين الإعدادات
scrcpy-smart --reset

# 4. جرب الاتصال اليدوي
scrcpy-smart --select
```

### المشكلة: الاتصال اللاسلكي يفشل

**الحلول:**
```bash
# 1. تحقق من الشبكة
ping 192.168.1.100

# 2. افحص الشبكة
scrcpy-smart --scan

# 3. اتصل بـ IP محدد
scrcpy-smart --ip 192.168.1.100

# 4. استخدم منفذ مختلف
scrcpy-smart --port 5556
```

### المشكلة: تقطيع أو بطء

**الحلول:**
```bash
# 1. قلل الجودة
scrcpy-smart --quality low

# 2. قلل الدقة
scrcpy-smart --quality medium

# 3. استخدم ملف توفير البطارية
scrcpy-smart --profile battery

# 4. تحقق من الشبكة
scrcpy-smart --perf
```

### المشكلة: الصوت لا يعمل

**الحلول:**
```bash
# 1. تأكد من عدم تعطيل الصوت
scrcpy-smart  # بدون --no-audio

# 2. أعد تشغيل الاتصال
scrcpy-smart --reset
scrcpy-smart
```

---

## نصائح وحيل

### 1. الاتصال السريع

أضف alias إلى `.bashrc` أو `.zshrc`:
```bash
alias phone='scrcpy-smart'
alias work='scrcpy-smart --device work-phone'
alias tablet='scrcpy-smart --device tablet --profile demo'
```

### 2. الإعدادات المثلى

للشبكات القوية:
```bash
scrcpy-smart --quality ultra --fullscreen
```

للشبكات الضعيفة:
```bash
scrcpy-smart --quality low --profile battery
```

### 3. الأتمتة

أنشئ سكريبت للمهام المتكررة:
```bash
#!/bin/bash
# deploy.sh

echo "🚀 Deploying to all devices..."
scrcpy-smart --push app.apk
scrcpy-smart --batch 'pm install -r /sdcard/app.apk'
scrcpy-smart --batch 'am start -n com.example.app/.MainActivity'
echo "✅ Done!"
```

### 4. المراقبة المستمرة

للجلسات الطويلة:
```bash
scrcpy-smart --daemon --notify
```

---

## الأوامر السريعة المرجعية

```bash
# الأساسيات
scrcpy-smart                    # اتصال عادي
scrcpy-smart --help            # المساعدة
scrcpy-smart --version         # الإصدار

# الأجهزة
scrcpy-smart --list            # عرض الأجهزة
scrcpy-smart --save NAME       # حفظ جهاز
scrcpy-smart --device NAME     # اتصال بجهاز
scrcpy-smart --devices         # عرض المحفوظة

# الملفات
scrcpy-smart --profile gaming  # ملف ألعاب
scrcpy-smart --quality high    # جودة عالية

# المراقبة
scrcpy-smart --monitor         # مراقبة
scrcpy-smart --daemon          # خدمة خلفية
scrcpy-smart --perf            # الأداء

# الإجراءات
scrcpy-smart --screenshot      # لقطة شاشة
scrcpy-smart --reboot          # إعادة تشغيل
scrcpy-smart --install app.apk # تثبيت

# متقدم
scrcpy-smart --qr              # QR code
scrcpy-smart --scan            # فحص الشبكة
scrcpy-smart --plugin NAME     # تشغيل إضافة
```

---

## الدعم والمساعدة

### الموارد

- **GitHub**: https://github.com/tareq-alomari/scrcpy-smart
- **Wiki**: https://github.com/tareq-alomari/scrcpy-smart/wiki
- **Issues**: https://github.com/tareq-alomari/scrcpy-smart/issues

### المساهمة

نرحب بمساهماتك! راجع `CONTRIBUTING.md` للتفاصيل.

---

**آخر تحديث**: 13 فبراير 2026  
**الإصدار**: v3.0.0  
**الحالة**: ✅ مكتمل
